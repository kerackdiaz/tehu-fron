import Panel from '../../components/templates/Panel/Panel';
import FormDevice from '../Formdevice/Formdevice';

const Parameterization: React.FC = () => {
  return (
    <Panel panelComponent={<FormDevice />} />
  );
};

export default Parameterization;